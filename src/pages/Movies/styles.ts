import {StyleSheet} from 'react-native';

export const moviesStyles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
  },
  loadingContainer: {
    flex: 1,
    justifyContent: 'center',
    alignContent: 'center',
  },
  flatlistContent: {
    gap: 30,
    width: '100%',
    padding: 20,
  },
});
